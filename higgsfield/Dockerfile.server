# Higgsfield API — Hono, run via tsx. Build context = repo ROOT.
#   docker compose build higgsfield-api
FROM node:24-alpine
WORKDIR /app
COPY . .
RUN npm ci
EXPOSE 8788
CMD ["npm", "run", "start", "-w", "@dasd/higgsfield-server"]
