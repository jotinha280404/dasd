# --- Higgsfield server ------------------------------------------------
PORT=8788

# --- Image generation (Phase 3) — real, works on the free tier --------
# Free key: https://aistudio.google.com/apikey  (same key pin-factory uses)
GEMINI_API_KEY=
# Optional override (default gemini-2.5-flash-image):
# IMAGE_MODEL=gemini-2.5-flash-image

# --- Video generation (Phase 4) — optional, paid ----------------------
# fal.ai (primary, pay-per-use). Without it the "Animate" button stays hidden.
# FAL_KEY=
# Veo via the Gemini key is an alternative premium path (reuses GEMINI_API_KEY).
