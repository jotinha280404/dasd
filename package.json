{
  "name": "dasd-workspace",
  "private": true,
  "version": "0.0.0",
  "description": "npm workspace root for the two web apps (orchestrator + higgsfield). The Python projects and the vanilla dashboard are NOT part of this workspace — see CLAUDE.md 'Framework exception'.",
  "engines": {
    "node": ">=24",
    "npm": ">=11"
  },
  "type": "module",
  "workspaces": [
    "packages/*",
    "orchestrator/shared",
    "orchestrator/server",
    "orchestrator/web",
    "higgsfield/shared",
    "higgsfield/server",
    "higgsfield/web"
  ],
  "scripts": {
    "dev:orchestrator": "concurrently -n orch-api,orch-web -c magenta,cyan \"npm run dev -w @dasd/orchestrator-server\" \"npm run dev -w @dasd/orchestrator-web\"",
    "dev:higgsfield": "concurrently -n higg-api,higg-web -c yellow,green \"npm run dev -w @dasd/higgsfield-server\" \"npm run dev -w @dasd/higgsfield-web\"",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "biome check .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.0",
    "concurrently": "^9.1.0",
    "typescript": "^5.8.0"
  }
}
